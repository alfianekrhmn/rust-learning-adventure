pub fn run() {
    let language = "Rust";
    let mut version = 1.0;
    println!("Bahasa yang digunakan adalah {} dengan version {}", language, version);
    version = 1.1;
    println!("Sekarang bahasa {} sudah updater version, menjadi version {}", language, version);
}
